const ProjectPage = () => {
  return (
    <div className="flex items-center justify-center w-full h-[93vh] bg-gray-200">
      <canvas className="w-[40%] bg-white h-[90%]"></canvas>
    </div>
  );
};

export default ProjectPage;
